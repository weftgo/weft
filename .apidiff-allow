# Acknowledged incompatible API changes since the last root tag, one
# exact apidiff line per entry (scripts/apidiff.sh). Pre-1.0 only: each
# entry is a deliberate, reviewed evolution. Reset this file when tagging.
#
# Reset at v0.2.0 (2026-09-19): the five lines acknowledged for the
# 0.1.0 → 0.2.0 cycle (Tool/RawTool trailing variadics, MaxResultBytes
# and Sequential widening to PolicyOption, RunFinish comparability) are
# in the tag; the gate now diffs against v0.2.0 and the list is empty.
